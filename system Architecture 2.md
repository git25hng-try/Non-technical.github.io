##Technical Communication 2

|Frontend|Backend|Error handling|Acceptance criteria|Design|Acceptance Criteria|
|--------|---------|-------------|-------------------|-------|------------------|
|Request username|GET/CHARACTERS- retrieves numeric, alphabetical and special characters|1)If username >8, frontend highlights topmost in-line validation criteria under the username bar 2) If username excludes a criteria, the in-line validation drop down list criteria excluded is highlighted red|1) Username for device owner is a one time action 2)Frontend must display in-line validation drop down list under the username bar with username content criteria typed in red, when criteria it is met, in-line validation changes to green 2)Username must be ≤8 3) Username must have ≥1 special character, ≥1 numeric character ≥1 upper case character ≥1 lower case character 4) Only new users can select new username if they are not returning, while returning screen users select their username each time they want use device|Onboarding page has an attractive blend of ≤3 colours, preferrably, blue green and white|1) Colors should have independent color settings 2) Colors should be mild in temperature 3) Colours should overlap midway ofthe screen|
|Request user_id|GET/DEVICE_ID|1)If user_id is the same, frontend displays 'sorry, this username already exist'|1) Username must be unique 2) Username must be matched to device to generate unique user_id| |  |
|Request victual eye test|GET/FONT SIZES|Frontend returns the 'type words bar' empty if the wrong word is typed|1) Frontend should display 'Welcome + Username + new usernames (if any) and displays underneath the 'Welcome' message, 'Are you ready for your eye test?|Frontend displays alphanumeric font characters from font size 16 to font size 6 by users' input random selection 2) Frontend would display options of right and wrong words for users to select or users to input the words they can see.|1)The victual eye test background should be warm with the image of a blinking eye|1) Eye should blink randomly and not very often. 2) The eye should blink for a longer period when users input the wrong word|
